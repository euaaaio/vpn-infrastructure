# Caddy configuration for XHTTP TLS Self-Steal
# Domain: {{ xhttp_subdomain }}.{{ cloudflare_zone }}

# HTTPS endpoint (auto-obtains Let's Encrypt certificate)
{{ xhttp_subdomain }}.{{ cloudflare_zone }} {
    respond "Site under construction" 200
}

# Fallback endpoint (Xray will proxy here for non-XHTTP traffic)
:{{ xhttp_fallback_port }} {
    respond "Site under construction" 200
}
